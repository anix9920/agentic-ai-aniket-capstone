[project]
name = "cloudoptima-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=1.4.2",
    "python-dotenv>=1.2.2",
]
